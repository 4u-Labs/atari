jt.WebAudioSpeaker = function(mainElement) { "use strict"; this.connect = function(audioSocket) { audioSocket.connectMonitor(this); }; this.connectPeripherals = function(pScreen) { screen = pScreen; }; this.connectAudioSignal = function(pAudioSignal) { if (audioSignal.indexOf(pAudioSignal) >= 0) return; jt.Util.arrayAdd(audioSignal, pAudioSignal); updateResamplingFactors(); }; this.disconnectAudioSignal = function(pAudioSignal) { if (audioSignal.indexOf(pAudioSignal) < 0) return; jt.Util.arrayRemoveAllElement(audioSignal, pAudioSignal); updateResamplingFactors(); }; this.powerOn = function() { createAudioContext(); if (!processor) return; registerUnlockOnTouchIfNeeded(); this.unpause(); }; this.powerOff = function() { this.pause(); if (audioContext) audioContext.close(); audioContext = processor = undefined; }; this.mute = function () { mute = true; }; this.unMute = function () { mute = false; }; this.pause = function () { if (processor) processor.disconnect(); }; this.unpause = function () { if (processor) processor.connect(audioContext.destination); }; this.toggleBufferBaseSize = function() { if (!audioContext) return screen.showOSD("Audio is DISABLED", true, true); bufferBaseSize = ((bufferBaseSize + 2) % 8) - 1; this.pause(); createProcessor(); this.unpause(); screen.showOSD("Audio Buffer size: " + (bufferBaseSize === -1 ? "Auto (" + bufferSize + ")" : bufferBaseSize === 0 ? "Browser (" + bufferSize + ")" : bufferSize), true); prefs.audioBufferBase = bufferBaseSize; Javatari.userPreferences.setDirty(); }; this.getControlReport = function(control) { return { label: bufferBaseSize === -2 ? "OFF" : bufferBaseSize === -1 ? "Auto" : bufferBaseSize === 0 ? "Browser" : bufferSize, active: bufferBaseSize > 0 }; }; function determineAutoBufferBaseSize() { return jt.Util.isMobileDevice() ? jt.Util.browserInfo().name === "CHROME" && !jt.Util.isIOSDevice() ? 4 : 3 : 2; } function determineBrowserDefaultBufferBaseSize() { return jt.Util.browserInfo().name === "SAFARI" || jt.Util.isIOSDevice() ? determineAutoBufferBaseSize() : 0; } var createAudioContext = function() { if (bufferBaseSize === -2 || Javatari.AUDIO_MONITOR_BUFFER_SIZE === 0) { jt.Util.warning("Audio disabled in configuration"); return; } try { var constr = (window.AudioContext || window.webkitAudioContext || window.WebkitAudioContext); if (!constr) throw new Error("WebAudio API not supported by the browser"); audioContext = new constr(); jt.Util.log("Speaker AudioContext created. Sample rate: " + audioContext.sampleRate + (audioContext.state ? ", " + audioContext.state : "")); createProcessor(); } catch(ex) { jt.Util.error("Could not create AudioContext. Audio DISABLED!\n" + ex); } }; var createProcessor = function() { try { var baseSize = bufferBaseSize === -1 ? determineAutoBufferBaseSize() : bufferBaseSize === 0 ? determineBrowserDefaultBufferBaseSize() : bufferBaseSize; var totalSize = Javatari.AUDIO_MONITOR_BUFFER_SIZE > 0 ? Javatari.AUDIO_MONITOR_BUFFER_SIZE : baseSize > 0 ? jt.Util.exp2(jt.Util.log2((audioContext.sampleRate + 14000) / 22050) | 0) * jt.Util.exp2(baseSize - 1) * 256 : 0; processor = audioContext.createScriptProcessor(totalSize, 1, 1); processor.onaudioprocess = onAudioProcess; bufferSize = processor.bufferSize; updateResamplingFactors(); jt.Util.log("Audio Processor buffer size: " + processor.bufferSize); } catch(ex) { jt.Util.error("Could not create ScriptProcessorNode. Audio DISABLED!\n" + ex); } }; function registerUnlockOnTouchIfNeeded() { if (processor && (!audioContext.state || audioContext.state === "suspended")) { mainElement.addEventListener("touchend", unlockAudioContext, true); mainElement.addEventListener("mousedown", unlockAudioContext, true); mainElement.addEventListener("keydown", unlockAudioContext, true); jt.Util.log("Speaker Audio Context resume event registered"); screen.speakerUnlockStateUpdate(false); } function unlockAudioContext() { mainElement.removeEventListener("touchend", unlockAudioContext, true); mainElement.removeEventListener("mousedown", unlockAudioContext, true); mainElement.removeEventListener("keydown", unlockAudioContext, true); var ex; try { audioContext.resume().then(function () { jt.Util.log('Speaker Audio Context resumed!'); }); } catch (e) { ex = e; } var source = audioContext.createBufferSource(); source.buffer = audioContext.createBuffer(1, 1, 22050); source.connect(audioContext.destination); source.start(0); if (ex) jt.Util.log("Audio Context unlocked!"); screen.speakerUnlockStateUpdate(true); } } function updateResamplingFactors() { if (!processor) return; resamplingFactor.length = audioSignal.length; resamplingLeftOver.length = audioSignal.length; for (var i = 0; i < audioSignal.length; i++) { resamplingFactor[i] = audioSignal[i].getSampleRate() / audioContext.sampleRate; resamplingLeftOver[i] = 0; audioSignal[i].setAudioMonitorBufferSize((resamplingFactor[i] * bufferSize) | 0); } } function onAudioProcess(event) { var outputBuffer = event.outputBuffer.getChannelData(0); var outputBufferSize = outputBuffer.length; for (var j = outputBufferSize - 1; j >= 0; j = j - 1) outputBuffer[j] = 0; if (audioSignal.length === 0) return; for (var i = audioSignal.length - 1; i >= 0; i = i - 1) { var resampFactor = resamplingFactor[i]; var input = audioSignal[i].retrieveSamples((outputBufferSize * resampFactor + resamplingLeftOver[i]) | 0, mute); var inputBuffer = input.buffer; var inputBufferSize = input.bufferSize; var s = input.start + resamplingLeftOver[i]; var d = 0; while (d < outputBufferSize) { outputBuffer[d] += inputBuffer[s | 0]; d = d + 1; s = s + resampFactor; if (s >= inputBufferSize) s = s - inputBufferSize; } resamplingLeftOver[i] = s - (s | 0); } } var screen; var audioSignal = []; this.signals = audioSignal; var resamplingFactor = []; var resamplingLeftOver = []; var prefs = Javatari.userPreferences.current; var bufferBaseSize = Javatari.AUDIO_MONITOR_BUFFER_BASE === -3 ? prefs.audioBufferBase : Javatari.AUDIO_MONITOR_BUFFER_BASE; var audioContext; var bufferSize; var processor; var mute = false; };