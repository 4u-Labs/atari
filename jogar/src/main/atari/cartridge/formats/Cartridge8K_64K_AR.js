jt.Cartridge8K_64K_AR = function(rom, format) { "use strict"; function init(self) { self.rom = rom; self.format = format; bytes = jt.Util.arrayFill(new Array(4 * BANK_SIZE)); self.bytes = bytes; loadBIOS(); for (var i = 0; i < 31; ++i) randomSeeds[i] = (Math.random() * 256) | 0; } this.powerOn = function() { setControlRegister(0x00); tapeOffset = 0; }; this.connectBus = function(pBus) { bus = pBus; }; this.read = function(address) { if (maskedAddress < BANK_SIZE) return bytes[bank0AddressOffset + maskedAddress]; else return bytes[bank1AddressOffset + maskedAddress - BANK_SIZE]; }; this.write = function(address, b) { if (bank1AddressOffset === BIOS_BANK_OFFSET && maskedAddress >= BIOS_INT_EMUL_LOAD_HOTSPOT && maskedAddress < BIOS_INT_EMUL_LOAD_HOTSPOT + 256) { loadPart(maskedAddress - BIOS_INT_EMUL_LOAD_HOTSPOT); } }; this.performBankSwitchOnMonitoredAccess = function(address) { maskedAddress = address & ADDRESS_MASK; address &= 0x1fff; if (address === 0x1ff8) { setControlRegister(valueToWrite); return; } if (addressChangeCountdown > 0) { if (address !== lastAddress) { lastAddress = address; if (--addressChangeCountdown === 0) { if ((address & CHIP_MASK) === CHIP_SELECT) { if (maskedAddress < BANK_SIZE) bytes[bank0AddressOffset + maskedAddress] = valueToWrite; else if (bank1AddressOffset < BIOS_BANK_OFFSET) bytes[bank1AddressOffset + maskedAddress - BANK_SIZE] = valueToWrite; } } } return; } if (((address & CHIP_MASK) === CHIP_SELECT) && maskedAddress <= 0x00ff) { valueToWrite = maskedAddress; if (writeEnabled) { lastAddress = address; addressChangeCountdown = 5; } } }; var setControlRegister = function(controlRegister) { var banksConfig = (controlRegister >> 2) & 0x07; switch (banksConfig) { case 0: bank0AddressOffset = 2 * BANK_SIZE; bank1AddressOffset = BIOS_BANK_OFFSET; break; case 1: bank0AddressOffset = 0 * BANK_SIZE; bank1AddressOffset = BIOS_BANK_OFFSET; break; case 2: bank0AddressOffset = 2 * BANK_SIZE; bank1AddressOffset = 0 * BANK_SIZE; break; case 3: bank0AddressOffset = 0 * BANK_SIZE; bank1AddressOffset = 2 * BANK_SIZE; break; case 4: bank0AddressOffset = 2 * BANK_SIZE; bank1AddressOffset = BIOS_BANK_OFFSET; break; case 5: bank0AddressOffset = 1 * BANK_SIZE; bank1AddressOffset = BIOS_BANK_OFFSET; break; case 6: bank0AddressOffset = 2 * BANK_SIZE; bank1AddressOffset = 1 * BANK_SIZE; break; case 7: bank0AddressOffset = 1 * BANK_SIZE; bank1AddressOffset = 2 * BANK_SIZE; break; default: throw new Error("Invalid bank configuration"); } addressChangeCountdown = 0; writeEnabled = (controlRegister & 0x02) !== 0; biosRomPower = (controlRegister & 0x01) === 0; }; var loadPart = function(part) { var tapeRewound = false; do { if (tapeOffset > rom.content.length - 1) { if (tapeRewound) { if (part === 0) bus.getTia().getVideoOutput().showOSD("Could not load Tape from Start. Not a Start Tape ROM!", true); else bus.getTia().getVideoOutput().showOSD("Could not find next Part to load in Tape!", true); signalPartLoadedOK(false); return; } tapeOffset = 0; tapeRewound = true; } if (jt.Cartridge8K_64K_AR.peekPartNoOnTape(rom.content, tapeOffset) === part) { if (part === 0) bus.getTia().getVideoOutput().showOSD("Loaded Tape from Start", true); else bus.getTia().getVideoOutput().showOSD("Loaded next Part from Tape", true); loadNextPart(); return; } else { tapeOffset += PART_SIZE; } } while(true); }; var loadNextPart = function() { loadHeaderData(); loadPagesIntoBanks(); patchPartDataIntoBIOS(); }; var loadHeaderData = function() { jt.Util.arrayCopy(rom.content, tapeOffset + 4 * BANK_SIZE, header, 0, header.length); romStartupAddress = (header[1] << 8) | (header[0] & 0xff); romControlRegister = header[2]; romPageCount = header[3]; romChecksum = header[4]; romMultiLoadIndex = header[5]; romProgressBarSpeed = (header[7] << 8) | (header[6] & 0xff); romPageOffsets = jt.Util.arrayFill(new Array(romPageCount), 0); jt.Util.arrayCopy(header, 16, romPageOffsets, 0, romPageCount); }; var loadPagesIntoBanks = function() { jt.Util.arrayFillSegment(bytes, 7 * PAGE_SIZE, 8 * PAGE_SIZE - 1, 0); var romOffset = tapeOffset; for (var i = 0, len = romPageOffsets.length; i < len; i++) { var pageInfo = romPageOffsets[i]; var bankOffset = (pageInfo & 0x03) * BANK_SIZE; var pageOffset = (pageInfo >> 2) * PAGE_SIZE; if (bankOffset + pageOffset + 255 < BIOS_BANK_OFFSET) jt.Util.arrayCopy(rom.content, romOffset, bytes, bankOffset + pageOffset, PAGE_SIZE); romOffset += PAGE_SIZE; } tapeOffset += PART_SIZE; }; var patchPartDataIntoBIOS = function() { bytes[BIOS_BANK_OFFSET + BIOS_INT_CONTROL_REG_ADDR - 0xf800] = romControlRegister; bytes[BIOS_BANK_OFFSET + BIOS_INT_PART_NO_ADDR - 0xf800] = romMultiLoadIndex; bytes[BIOS_BANK_OFFSET + BIOS_INT_RANDOM_SEED_ADDR - 0xf800] = randomSeeds[currentRandomSeed++]; if (currentRandomSeed > 30) currentRandomSeed = 0; bytes[BIOS_BANK_OFFSET + BIOS_INT_START_ADDR - 0xf800] = romStartupAddress & 0xff; bytes[BIOS_BANK_OFFSET + BIOS_INT_START_ADDR + 1 - 0xf800] = (romStartupAddress >> 8) & 0xff; signalPartLoadedOK(true); }; var signalPartLoadedOK = function(ok) { bytes[BIOS_BANK_OFFSET + BIOS_INT_PART_LOADED_OK - 0xf800] = (ok ? 1 : 0); }; var loadBIOS = function() { var bios = jt.Util.uncompressStringBase64ToInt8BitArray(STARPATH_BIOS); jt.Util.arrayCopy(bios, 0, bytes, BIOS_BANK_OFFSET, BANK_SIZE); }; var bus; var bytes; var bank0AddressOffset = 0; var bank1AddressOffset = 0; var valueToWrite = 0; var writeEnabled = false; var lastAddress = -1; var addressChangeCountdown = 0; var biosRomPower = false; var romStartupAddress = 0; var romControlRegister = 0; var romPageCount = 0; var romChecksum = 0; var romMultiLoadIndex = 0; var romProgressBarSpeed = 0; var romPageOffsets; var randomSeeds = new Array(31); var currentRandomSeed = 0; var tapeOffset = 0; var HEADER_SIZE = 256; var header = jt.Util.arrayFill(new Array(HEADER_SIZE), 0); var maskedAddress; var BIOS_INT_PART_NO_ADDR = 0xfb00; var BIOS_INT_CONTROL_REG_ADDR = 0xfb01; var BIOS_INT_START_ADDR = 0xfb02; var BIOS_INT_RANDOM_SEED_ADDR = 0xfb04; var BIOS_INT_PART_LOADED_OK = 0xfb05; var BIOS_INT_EMUL_LOAD_HOTSPOT = 0x0c00; var PAGE_SIZE = 256; var BANK_SIZE = 8 * PAGE_SIZE; var BIOS_BANK_OFFSET = 3 * BANK_SIZE; var PART_SIZE = 4 * BANK_SIZE + HEADER_SIZE; var STARPATH_BIOS = "7dSxCsIwEAbgv6niGkeddPVZ8kCOXc43yCIokkGIUN+gLxAoZHTxHRxjYq2xk7vSIPS75bb7uYNTuOJWu/bod3iU42BzUTiBe9sTzSj" + "ToBnNBVxfQz/nQ+2NhA2a05KYmhhjmxhoQZymxGil8gpeesOdyioW5DN25yxsiri3chQOUO1WeCSI/hPx9AJ/m/576KROMUhlfdE4dQ+AfJoPNBikgOZdLw=="; var ADDRESS_MASK = 0x0fff; var CHIP_MASK = 0x1000; var CHIP_SELECT = 0x1000; this.saveState = function() { return { f: this.format.name, r: this.rom.saveState(true), b: jt.Util.compressInt8BitArrayToStringBase64(bytes), b0o: bank0AddressOffset, b1o: bank1AddressOffset, vw: valueToWrite, we: writeEnabled, la: lastAddress, ac: addressChangeCountdown, bp: biosRomPower, rs: romStartupAddress, rc: romControlRegister, rp: romPageCount, rk: romChecksum, rm: romMultiLoadIndex, rb: romProgressBarSpeed, ro: romPageOffsets, to: tapeOffset, rnd: jt.Util.storeInt8BitArrayToStringBase64(randomSeeds), rnc: currentRandomSeed }; }; this.loadState = function(state) { this.format = jt.CartridgeFormats[state.f]; this.rom = rom = jt.ROM.loadState(state.r); bytes = jt.Util.uncompressStringBase64ToInt8BitArray(state.b, bytes); this.bytes = bytes; bank0AddressOffset = state.b0o; bank1AddressOffset = state.b1o; valueToWrite = state.vw; writeEnabled = state.we; lastAddress = state.la; addressChangeCountdown = state.ac; biosRomPower = state.bp; romStartupAddress = state.rs; romControlRegister = state.rc; romPageCount = state.rp; romChecksum = state.rk; romMultiLoadIndex = state.rm; romProgressBarSpeed = state.rb; romPageOffsets = state.ro; tapeOffset = state.to; jt.Util.restoreStringBase64ToInt8BitArray(state.rnd, randomSeeds); currentRandomSeed = state.rnc; }; if (rom) init(this); }; jt.Cartridge8K_64K_AR.prototype = jt.CartridgeBankedByBusMonitoring.base; jt.Cartridge8K_64K_AR.recreateFromSaveState = function(state, prevCart) { var cart = prevCart || new jt.Cartridge8K_64K_AR(); cart.loadState(state); return cart; }; jt.Cartridge8K_64K_AR.HEADER_SIZE = 256; jt.Cartridge8K_64K_AR.PAGE_SIZE = 256; jt.Cartridge8K_64K_AR.BANK_SIZE = 8 * jt.Cartridge8K_64K_AR.PAGE_SIZE; jt.Cartridge8K_64K_AR.PART_SIZE = 4 * jt.Cartridge8K_64K_AR.BANK_SIZE + jt.Cartridge8K_64K_AR.HEADER_SIZE; jt.Cartridge8K_64K_AR.peekPartNoOnTape = function(tapeContent, tapeOffset) { return tapeContent[tapeOffset + 4*jt.Cartridge8K_64K_AR.BANK_SIZE + 5]; }; jt.Cartridge8K_64K_AR.checkTape = function(rom) { if (jt.Cartridge8K_64K_AR.peekPartNoOnTape(rom.content, 0) === 0) return true; jt.Util.warning("Wrong Supercharger Tape Part ROM! Please load a Full Tape ROM file"); return false; };