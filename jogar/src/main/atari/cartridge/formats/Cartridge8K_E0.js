jt.Cartridge8K_E0 = function(rom, format) { "use strict"; function init(self) { self.rom = rom; self.format = format; bytes = rom.content; self.bytes = bytes; } this.read = function(address) { var maskedAddress = maskAddress(address); if (maskedAddress < 0x0400) return bytes[slice0AddressOffset + maskedAddress]; if (maskedAddress < 0x0800) return bytes[slice1AddressOffset + maskedAddress - 0x0400]; if (maskedAddress < 0x0c00) return bytes[slice2AddressOffset + maskedAddress - 0x0800]; return bytes[0x1000 + maskedAddress]; }; this.write = function(address, val) { maskAddress(address); }; var maskAddress = function(address) { var maskedAddress = address & ADDRESS_MASK; if (maskedAddress >= 0x0fe0 && maskedAddress <= 0x0ff7) { if ( maskedAddress <= 0x0fe7) slice0AddressOffset = (maskedAddress - 0x0fe0) * 0x0400; else if ( maskedAddress <= 0x0fef) slice1AddressOffset = (maskedAddress - 0x0fe8) * 0x0400; else if ( maskedAddress <= 0x0ff7) slice2AddressOffset = (maskedAddress - 0x0ff0) * 0x0400; } return maskedAddress; }; this.saveState = function() { return { f: this.format.name, r: this.rom.saveState(), b: jt.Util.compressInt8BitArrayToStringBase64(bytes), s0: slice0AddressOffset, s1: slice1AddressOffset, s2: slice2AddressOffset }; }; this.loadState = function(state) { this.format = jt.CartridgeFormats[state.f]; this.rom = jt.ROM.loadState(state.r); bytes = jt.Util.uncompressStringBase64ToInt8BitArray(state.b, bytes); this.bytes = bytes; slice0AddressOffset = state.s0; slice1AddressOffset = state.s1; slice2AddressOffset = state.s2; }; var bytes; var slice0AddressOffset = 0; var slice1AddressOffset = 0; var slice2AddressOffset = 0; var ADDRESS_MASK = 0x0fff; if (rom) init(this); }; jt.Cartridge8K_E0.prototype = jt.Cartridge.base; jt.Cartridge8K_E0.recreateFromSaveState = function(state, prevCart) { var cart = prevCart || new jt.Cartridge8K_E0(); cart.loadState(state); return cart; };