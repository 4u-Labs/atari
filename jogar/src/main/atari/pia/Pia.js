jt.Pia = function() { "use strict"; this.powerOn = function() { }; this.powerOff = function() { }; this.clockPulse = function() { if (--timerCount <= 0) decrementTimer(); }; this.connectBus = function(aBus) { bus = aBus; }; this.read = function(address) { var reg = address & ADDRESS_MASK; if (reg === 0x04 || reg === 0x06) { readFromINTIM(); return INTIM; } if (reg === 0x00) return SWCHA; if (reg === 0x02) return SWCHB; if (reg === 0x01) return SWACNT; if (reg === 0x03) return SWBCNT; if (reg === 0x05 || reg === 0x07) return INSTAT; return 0; }; this.write = function(address, i) { var reg = address & ADDRESS_MASK; if (reg === 0x04) { TIM1T = i; setTimerInterval(i, 1); return; } if (reg === 0x05) { TIM8T = i; setTimerInterval(i, 8); return; } if (reg === 0x06) { TIM64T = i; setTimerInterval(i, 64); return; } if (reg === 0x07) { T1024T = i; setTimerInterval(i, 1024); return; } if (reg === 0x02) { swchbWrite(i); return; } if (reg === 0x03) { SWBCNT = i; debugInfo(">>>> Ineffective Write to PIA SWBCNT: " + i); return; } if (reg === 0x00) { debugInfo(">>>> Unsupported Write to PIA SWCHA: " + i); return; } if (reg === 0x01) { debugInfo(">>>> Unsupported Write to PIA SWACNT " + i); return; } return 0; }; var decrementTimer = function() { if (--INTIM < 0) { INSTAT |= 0xc0; INTIM = 0xff; timerCount = currentTimerInterval = 1; } else timerCount = currentTimerInterval; }; var setTimerInterval = function(value, interval) { INTIM = value; INSTAT &= 0x3f; timerCount = currentTimerInterval = lastSetTimerInterval = interval; decrementTimer(); }; var readFromINTIM = function() { INSTAT &= 0xbf; if (currentTimerInterval === 1) timerCount = currentTimerInterval = lastSetTimerInterval; }; var swchbWrite = function(val) { SWCHB = (SWCHB & 0xcb) | (val & 34); }; var debugInfo = function(str) { if (self.debug) jt.Util.log(str); }; var controls = jt.ConsoleControls; this.controlStateChanged = function(control, state) { switch (control) { case controls.JOY0_UP: if (state) SWCHA &= 0xef; else SWCHA |= 0x10; return; case controls.JOY0_DOWN: if (state) SWCHA &= 0xdf; else SWCHA |= 0x20; return; case controls.PADDLE1_BUTTON: case controls.JOY0_LEFT: if (state) SWCHA &= 0xbf; else SWCHA |= 0x40; return; case controls.PADDLE0_BUTTON: case controls.JOY0_RIGHT: if (state) SWCHA &= 0x7f; else SWCHA |= 0x80; return; case controls.JOY1_UP: if (state) SWCHA &= 0xfe; else SWCHA |= 0x01; return; case controls.JOY1_DOWN: if (state) SWCHA &= 0xfd; else SWCHA |= 0x02; return; case controls.JOY1_LEFT: if (state) SWCHA &= 0xfb; else SWCHA |= 0x04; return; case controls.JOY1_RIGHT: if (state) SWCHA &= 0xf7; else SWCHA |= 0x08; return; case controls.RESET: if (state) SWCHB &= 0xfe; else SWCHB |= 0x01; return; case controls.SELECT: if (state) SWCHB &= 0xfd; else SWCHB |= 0x02; return; } if (!state) return; switch (control) { case controls.BLACK_WHITE: if ((SWCHB & 0x08) == 0) SWCHB |= 0x08; else SWCHB &= 0xf7; bus.getTia().getVideoOutput().showOSD((SWCHB & 0x08) != 0 ? "COLOR" : "B/W", true); return; case controls.DIFFICULTY0: if ((SWCHB & 0x40) == 0) SWCHB |= 0x40; else SWCHB &= 0xbf; bus.getTia().getVideoOutput().showOSD((SWCHB & 0x40) != 0 ? "P1 Expert" : "P1 Novice", true); return; case controls.DIFFICULTY1: if ((SWCHB & 0x80) == 0) SWCHB |= 0x80; else SWCHB &= 0x7f; bus.getTia().getVideoOutput().showOSD((SWCHB & 0x80) != 0 ? "P2 Expert" : "P2 Novice", true); return; } }; this.controlsStateReport = function(report) { report[controls.BLACK_WHITE] = (SWCHB & 0x08) === 0; report[controls.DIFFICULTY0] = (SWCHB & 0x40) !== 0; report[controls.DIFFICULTY1] = (SWCHB & 0x80) !== 0; report[controls.SELECT] = (SWCHB & 0x02) === 0; report[controls.RESET] = (SWCHB & 0x01) === 0; }; this.saveState = function() { return { t: timerCount, c: currentTimerInterval, l: lastSetTimerInterval, SA: SWCHA, SAC: SWACNT, SB: SWCHB, SBC: SWBCNT, IT: INTIM, IS: INSTAT, T1: TIM1T, T8: TIM8T, T6: TIM64T, T2: T1024T }; }; this.loadState = function(state) { timerCount = state.t; currentTimerInterval = state.c; lastSetTimerInterval = state.l; SWACNT = state.SAC; SWCHB = state.SB; SWBCNT = state.SBC; INTIM = state.IT; INSTAT = state.IS; TIM1T = state.T1; TIM8T = state.T8; TIM64T = state.T6; T1024T = state.T2; }; this.debug = false; var bus; var timerCount = 1024; var currentTimerInterval = 1024; var lastSetTimerInterval = 1024; var SWCHA= 0xff; var SWACNT = 0; var SWCHB = 0x0b; var SWBCNT = 0; var INTIM = (Math.random() * 256) | 0 ; var INSTAT = 0; var TIM1T = 0; var TIM8T = 0; var TIM64T = 0; var T1024T = 0; var ADDRESS_MASK = 0x0007; };